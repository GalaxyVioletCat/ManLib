<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :book_title %>
  </div>



  <div class="field">
    <%= form.label :autors %>
    <%= form.select :autors, Author.all.collect {|x| [x.name]},{:include_blank => 'Select One'} , { :multiple => true} %>
  </div>

  <div class="field">
    <%= form.label :img_url %>
    <%= form.text_field :img_url, id: :book_img_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
